# This doc has moved

The registration backend was rebuilt on Supabase (Postgres + an Edge
Function), replacing the Google Sheets / Apps Script setup this file used
to describe. See **[SUPABASE_SETUP.md](SUPABASE_SETUP.md)** for the current
setup instructions.

The old Google Sheet ("REEV 4.0 Registrations") and its Apps Script are no
longer used by the live site and can be left alone or deleted at your
convenience — nothing on the site talks to them anymore.
